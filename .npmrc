# Make the engines.node floor in package.json a hard install failure rather than a
# warning. sharp 0.35 (see README "Dependency overrides") requires Node >= 20.9, and
# installing on an older Node otherwise fails later with a confusing native-binary error.
engine-strict=true
